import Agent from '../../../components/Agent';
import React from 'react';

const page = () => {
  return (
     <>
     <Agent userName={''} userId={''} type={''} />
     </>
  )
}

export default page;